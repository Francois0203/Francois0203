# Utils

This directory contains utility functions and components for common operations in the Core-UI library.

## HexToRGB

Converts CSS color values to RGB array format.

**Features:**
- Supports hex color codes (#RGB, #RRGGBB)
- Supports rgb/rgba CSS strings
- Reads CSS custom properties
- Provides fallback default color

**Returns:** RGB array `[r, g, b]`